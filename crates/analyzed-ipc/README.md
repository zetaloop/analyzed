# analyzed-ipc

Protocol types and transports for the channel between the analyzed daemon and its clients: Unix sockets on Linux and macOS, named pipes on Windows.

Part of [analyzed](https://github.com/zetaloop/analyzed), a shared rust-analyzer daemon.
