# analyzed-daemon

The daemon behind analyzed: it listens on the platform endpoint, manages client sessions, and attaches sessions with a compatible toolchain and Cargo configuration to a shared analysis backend.

Part of [analyzed](https://github.com/zetaloop/analyzed), a shared rust-analyzer daemon.
